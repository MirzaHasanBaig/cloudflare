pipeline {
    agent any
    environment {
        cloudflare = credentials('cloudflare')
    }
    stages {
        stage('Build') {
            steps {
                bat "terraform apply --var token=$cloudflare -auto-approve"
            }
        }
    }
}